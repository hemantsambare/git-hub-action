name: scecrete practice
on:
  push
jobs:
  use-secret:
    secret-file:
      runs-on: ubuntu-latest
      steps:
        - name: print secret
          run: echo "The secret is \${{secrets.SECRET}}"

